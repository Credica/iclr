# 持续强化学习中的贝尔曼条件谱可塑性失配

## 1. 核心思想

持续强化学习中的负向迁移不能只用“网络还剩多少可塑性”解释。旧任务训练会重塑评价网络的学习谱，使网络沿少数函数方向更新得很快、沿另一些方向更新得极慢；新任务则通过自身的贝尔曼算子，规定当前控制学习真正需要修正的函数方向。当新任务的贝尔曼相关更新子空间落入继承评价网络的小特征值方向时，这些方向即使仍然可以表示，也难以在有限训练预算内学会，从而产生负向迁移。

我们将这一机制称为：

> **贝尔曼条件谱可塑性失配（Bellman-conditioned spectral plasticity mismatch）**

核心因果链为：

```text
旧任务 A 塑造评价网络的学习谱
             ↓
任务切换后，网络谱与任务 B 的贝尔曼相关函数方向失配
             ↓
B 的重要更新落入弱学习方向，谱负担持续升高
             ↓
评价网络拟合变慢，策略改进受阻，形成负向迁移
```

这个观点统一了两个过去相对分离的问题：

- 可塑性研究回答“网络现在沿哪些方向还能学、学得多快”；
- 谱贝尔曼研究回答“哪些函数方向对当前控制任务重要”；
- 我们研究二者在顺序任务切换后的相互作用。

---

## 2. 论文定位与三阶段研究逻辑

这项工作应定位为一篇以**重新思考持续强化学习负向迁移机制**为主的论文，而不是单纯的方法论文。论文首先揭示传统秩、休眠率和无权覆盖率没有捕捉到的谱条件化失败，再验证它能否跨任务对预测负向迁移，最后用因果干预把现象转化为解决方法。

| 阶段 | 目标 | 核心问题 | 证据形式 |
| --- | --- | --- | --- |
| 第一阶段 | 发现并验证现象 | 普通顺序微调是否使评价网络学习谱集中，并导致新任务相关方向难以学习？ | `sweep-into → push-wall` 三种子；从头训练、普通迁移、评价网络重置 |
| 第二阶段 | 验证普适性和预测性 | 同一个任务 A 之后，不同任务 B 的负向迁移强度能否由谱负担预测？ | 同一 A 检查点分支到 8 个 B；迁移差距与谱负担的跨任务关系 |
| 第三阶段 | 建立因果性并形成方法 | 在不改变当前价值函数输出的情况下，只改变学习谱，能否改变后续适应速度？ | 函数保持的定向谱提升、随机对照和反向抑制 |

暂定题目：

> **持续强化学习中的贝尔曼条件谱可塑性：学习谱集中如何导致负向迁移**

---

## 3. 研究背景与缺口

持续强化学习通常按顺序学习多个任务。已有研究发现，普通微调在某些新任务上会显著慢于从头训练，并从神经元休眠、特征秩下降、梯度退化和参数路径依赖等角度解释可塑性损失。[重置与蒸馏工作](https://arxiv.org/abs/2403.05066)进一步展示了评价网络历史对负向迁移的重要影响。

然而，“网络整体变得不容易训练”仍然不能完整解释两个关键现象：

1. 相似的训练历史可能只对某些后续任务造成严重负向迁移；
2. 特征秩、休眠率或梯度仍然正常时，控制学习依然可能失败。

另一方面，[谱贝尔曼方法](https://arxiv.org/abs/2507.13181)从固定任务出发，研究表示空间能否覆盖贝尔曼算子产生的关键函数方向。其闭合性对象可以写为：

$$
\mathcal{T}_B \mathcal{Q}_{\phi} \subseteq \mathcal{Q}_{\phi}.
$$

这说明高维表示本身并不等于有效表示；真正重要的是表示是否覆盖与控制有关的贝尔曼结构。但该工作没有研究任务从 $A$ 切换到 $B$ 后，任务 $A$ 塑造的网络学习谱是否仍适合任务 $B$。

因此，现有研究之间存在一个明确缺口：

> **持续强化学习不仅需要测量网络还剩多少可学习能力，还需要测量这些学习能力是否分布在新任务真正相关的函数方向上。**

---

## 4. 问题设定

考虑按顺序到来的两个任务 $A \rightarrow B$。任务 $A$ 训练结束后得到参数 $\theta_A$，随后在任务 $B$ 上继续微调得到 $\theta_{A\rightarrow B,t}$。以相同训练预算直接学习任务 $B$ 的参数记为 $\theta_{B,t}^{\mathrm{scratch}}$。

令 $S_g(t)$ 表示设置 $g$ 在任务 $B$ 上的成功率，定义归一化成功率曲线面积：

$$
\operatorname{AUC}_g(B;H)
=
\frac{1}{H}\int_0^H S_g(t)\,\mathrm{d}t.
$$

任务对的迁移差距定义为：

$$
\Delta_{A\rightarrow B}(H)
=
\operatorname{AUC}_{A\rightarrow B}(B;H)
-
\operatorname{AUC}_{\mathrm{scratch}}(B;H).
$$

其中 $\Delta_{A\rightarrow B}<0$ 表示负向迁移。我们的目标不是仅仅描述负向迁移是否发生，而是解释并预测 $\Delta_{A\rightarrow B}$ 为什么会随任务对而变化。

---

## 5. 两个需要匹配的谱对象

### 5.1 任务 B 的贝尔曼相关更新子空间

在任务 $B$ 的固定状态—动作锚点 $X_B=\{(s_i,a_i)\}_{i=1}^{n}$ 上，当前评价网络输出记为 $q_{\theta}(X_B)$。一次贝尔曼更新希望消除的函数残差为：

$$
d_B(\theta;X_B)
=
\mathcal{T}_B q_{\theta}(X_B)-q_{\theta}(X_B).
$$

对一组价值函数或局部参数变体 $\{\theta_r\}_{r=1}^{m}$，构造增广贝尔曼相关更新矩阵：

$$
D_B
=
\begin{bmatrix}
d_B(\theta_1;X_B) &
d_B(\theta_2;X_B) &
\cdots &
d_B(\theta_m;X_B)
\end{bmatrix}
\in \mathbb{R}^{n\times m}.
$$

$D_B$ 的主奇异方向描述任务 $B$ 当前需要评价网络改变的主要函数方向。为了进一步接近谱贝尔曼方法所研究的算子结构，还可以构造：

$$
\mathcal{B}_B
=
\begin{bmatrix}
\mathcal{T}_B q_{\theta_1}(X_B) &
\mathcal{T}_B q_{\theta_2}(X_B) &
\cdots &
\mathcal{T}_B q_{\theta_m}(X_B)
\end{bmatrix}.
$$

二者承担不同作用：

- $\mathcal{B}_B$ 刻画一族价值函数经过任务 $B$ 的贝尔曼算子后形成的结构谱；
- $D_B$ 刻画当前训练阶段实际需要消除的贝尔曼相关函数误差。

第一阶段主要使用 $D_B$ 研究局部学习动力学；第二阶段和最终算子级分析将进一步扩大 $\{\theta_r\}$ 的采样范围并分析 $\mathcal{B}_B$。

### 5.2 继承评价网络的切线学习谱

评价网络在锚点 $X_B$ 上对全部参数的雅可比矩阵为：

$$
J_{\theta}(X_B)
=
\frac{\partial q_{\theta}(X_B)}{\partial \theta}.
$$

对应的经验切线核为：

$$
K_{\theta}(X_B)
=
\frac{1}{p}J_{\theta}(X_B)J_{\theta}(X_B)^{\top}
=
U\Lambda U^{\top}
=
\sum_{i=1}^{n}\lambda_i u_i u_i^{\top},
$$

其中 $p$ 是参数维度，$u_i$ 是网络输出空间中的可学习函数方向，$\lambda_i$ 决定沿该方向的局部学习速度。大特征值方向更新快，小特征值方向更新慢。

我们把 $K_{\theta}$ 称为网络的**切线学习谱**。它比最后一层特征协方差更完整，因为它同时包含所有评价网络参数对输出的影响。

---

## 6. 为什么谱集中只有在任务条件下才产生负向迁移

将任务 $B$ 的一个相关更新方向分解到切线核特征向量上：

$$
d_B
=
\sum_{i=1}^{n}c_i u_i.
$$

在线性化梯度下降下，每个分量近似按如下速度衰减：

$$
c_i(t)
\approx
(1-\eta\lambda_i)^t c_i(0).
$$

因此，谱集中本身并不自动等于负向迁移：

- 如果任务 $B$ 的关键方向主要落在大特征值子空间，继承网络仍可快速学习；
- 如果这些方向主要落在小特征值子空间，网络虽然可以表示它们，却需要远多于正常训练预算的更新；
- 同一个任务 $A$ 形成的评价网络，对不同任务 $B$ 可以产生完全不同的迁移结果。

这给出了一个任务对相关的机制：

$$
\text{负向迁移强度}
\quad\text{由}\quad
\bigl(D_B,\,K_{\theta_A}\bigr)
\quad\text{的相对谱几何决定。}
$$

### 6.1 连续时间下的累计拟合代价

令 $E(t)$ 表示在锚点上的函数拟合误差。在局部线性化和梯度流近似下：

$$
\frac{\mathrm{d}E(t)}{\mathrm{d}t}
=
-K E(t),
$$

从而：

$$
E(t)=\exp(-Kt)E(0).
$$

当 $K$ 正定时，整个训练过程的累计平方误差具有闭式形式：

$$
\int_0^{\infty}\lVert E(t)\rVert_F^2\,\mathrm{d}t
=
\frac{1}{2}
\operatorname{Tr}
\left[
E(0)^{\top}K^{-1}E(0)
\right].
$$

这说明小特征值方向会被逆谱显著放大。我们的核心指标由此直接得到，而不是任意挑选的相关性统计量。

---

## 7. 核心指标：归一化逆谱负担

令切线核的平均特征值为：

$$
\bar{\lambda}(K)
=
\frac{\operatorname{Tr}(K)}{n}.
$$

使用相对岭参数 $\varepsilon\bar{\lambda}(K)$，定义贝尔曼条件逆谱负担：

$$
\mathcal{P}_B(K,D_B)
=
\frac{
\operatorname{Tr}
\left[
D_B^{\top}
\left(K+\varepsilon\bar{\lambda}(K)I\right)^{-1}
D_B
\right]
}{
\lVert D_B\rVert_F^2
}.
$$

为消除评价网络整体雅可比尺度的影响，使用无量纲版本：

$$
\widetilde{\mathcal{P}}_B(K,D_B)
=
\bar{\lambda}(K)\mathcal{P}_B(K,D_B).
$$

$\widetilde{\mathcal{P}}_B$ 越大，说明在排除整体更新尺度后，任务 $B$ 的相关函数方向越集中于当前网络的弱谱方向。本文将它简称为**归一化逆谱负担**。

为了描述整个任务 $B$ 训练阶段的适应难度，可以进一步定义前 $H$ 步的累计谱适应负担：

$$
\operatorname{SAB}_{A\rightarrow B}(H)
=
\frac{1}{H}
\int_0^H
\widetilde{\mathcal{P}}_B
\left(K_{\theta_{A\rightarrow B,t}},D_{B,t}\right)
\,\mathrm{d}t.
$$

核心预测为：

$$
\operatorname{SAB}_{A\rightarrow B}(H)\uparrow
\quad\Longrightarrow\quad
\Delta_{A\rightarrow B}(H)\downarrow.
$$

### 7.1 辅助指标及其作用边界

切线核的谱熵有效秩定义为：

$$
r_{\mathrm{ent}}(K)
=
\exp\left(-\sum_i p_i\log p_i\right),
\qquad
p_i=\frac{\lambda_i}{\sum_j\lambda_j}.
$$

它描述学习谱是否集中，但不包含任务 $B$ 的方向信息。

正则化覆盖率可以写为：

$$
C_B(K,D_B)
=
\frac{
\operatorname{Tr}
\left[
D_B^{\top}K
\left(K+\varepsilon\bar{\lambda}(K)I\right)^{-1}
D_B
\right]
}{
\lVert D_B\rVert_F^2
}.
$$

覆盖率主要回答“相关方向是否处在可达子空间中”，逆谱负担则进一步回答“这些方向在有限预算内能否快速学会”。因此，高覆盖率与高逆谱负担可以同时出现，这正是“可以表示但难以优化”的情形。

特征秩、神经元休眠比例和一般目标拟合仍然是必要对照，但它们分别只描述表示维度、激活状态和整体可训练性，不能单独刻画任务结构与网络学习谱的匹配程度。

---

## 8. 与谱贝尔曼方法的关系

| 谱贝尔曼方法 | 本研究 |
| --- | --- |
| 研究固定任务中的贝尔曼结构 | 研究顺序任务切换后的跨任务谱适应 |
| 关注表示空间对贝尔曼变换的闭合性 | 关注继承网络能否快速沿新任务相关方向更新 |
| 主要对象是贝尔曼变换后的价值函数族 | 主要对象是新任务贝尔曼相关子空间与评价网络切线谱的相对几何 |
| 说明哪些表示方向对控制有用 | 说明网络沿这些有用方向还能学多快 |
| 服务于表示学习与探索 | 解释并缓解持续强化学习中的负向迁移 |

二者的结合可以概括为：

$$
\boxed{
\text{新任务的贝尔曼相关函数结构}
\quad\longleftrightarrow\quad
\text{继承评价网络的切线学习谱}
}
$$

谱贝尔曼方法的核心启发是：控制学习依赖的不是任意高维方向，而是贝尔曼算子诱导的特定函数方向。我们的扩展是：在持续强化学习中，即使这些方向仍然存在于网络可表达空间中，只要它们落入继承网络的小特征值方向，就会形成优化时间尺度上的负向迁移。

因此，本文不是简单把谱贝尔曼方法应用到持续强化学习，而是把“贝尔曼表示闭合”进一步连接到“任务切换后的方向性学习速度”。

---

## 9. 经验测量的三个层次

### 9.1 在线最后层代理

训练时记录评价网络最后隐藏层特征矩阵 $\Phi$，计算特征秩、奇异值谱，以及 $D$ 在 $\Phi$ 张成空间中的对齐和覆盖率。它适合低成本追踪，但只描述最后层线性可表达性。

### 9.2 固定锚点上的完整雅可比分析

从检查点恢复完整评价网络，在固定任务锚点上计算全部参数的 $J$、$K$、谱熵有效秩、覆盖率和逆谱负担，并进行统一步数的局部拟合试验。这是第一阶段的主机制证据，因为它直接测量整个评价网络的局部学习动力学。

### 9.3 算子级增广谱分析

在更广的价值函数参数分布上采样 $\{Q_{\theta_r}\}$，构造 $\mathcal{B}_B$ 和 $D_B$，分析其主奇异子空间。这一层用于把经验局部机制进一步连接到谱贝尔曼方法的算子结构。

这三个层次不是相互替代关系，而是从低成本现象追踪、完整网络动力学到算子结构的递进证据。

### 9.4 仅分析 B 与联合分析 A+B

顺序任务天然包含历史任务和新任务，因此两种分析都合理：

- **仅分析 B**直接测量继承评价网络适应新任务 $B$ 的难度，是解释前向负向迁移的主对象；
- **A+B 联合分析**测量网络在保留历史任务结构的同时容纳新任务方向的能力，是持续学习整体闭合性的对象。

联合分析并不是统计错误，仅分析 B 也不是对历史任务的否定。二者分别回答“新任务为什么难学”和“顺序任务的联合结构如何演化”。同样，在统一网络结构、样本规模、参数变体规则和归一化方式下，各运行自身形成的残差矩阵可以比较，它刻画不同训练历史产生的内生贝尔曼几何；固定锚点分析则进一步把状态分布差异从网络谱差异中分离出来。

---

## 10. 第一阶段：单任务对三种子验证

### 10.1 实验设置

任务对为：

$$
\texttt{sweep-into}
\quad\longrightarrow\quad
\texttt{push-wall}.
$$

比较三种设置：

| 设置 | 作用 |
| --- | --- |
| 从头训练 | 直接从随机初始化学习 `push-wall`，给出没有旧任务路径依赖时的正常学习谱 |
| 普通迁移 | 先学习 `sweep-into`，再直接微调 `push-wall`，测量评价网络历史导致的负向迁移 |
| 评价网络重置 | 在任务边界重置两个评价网络并保留策略网络，检验负向迁移是否主要来自评价网络的谱路径依赖 |

每组使用 3 个种子，任务 $B$ 训练 300 万步。行为主结果使用每个最终检查点独立评估 50 个回合的成功率；机制分析使用共同锚点上的完整雅可比和统一的局部拟合试验。

### 10.2 行为负向迁移

最终 50 回合成功率为：

| 设置 | 种子 0 | 种子 1 | 种子 2 | 平均值 |
| --- | ---: | ---: | ---: | ---: |
| 从头训练 | 1.00 | 0.80 | 0.42 | 0.74 |
| 普通迁移 | 0.00 | 0.00 | 0.00 | 0.00 |
| 评价网络重置 | 1.00 | 0.54 | 0.26 | 0.60 |

普通迁移在 3/3 个种子上完全失败，而从头训练和评价网络重置都能学会任务。这说明负向迁移不是 `push-wall` 本身不可学习，并把关键路径依赖定位到继承的评价网络。

### 10.3 普通可塑性指标无法解释该失败

任务结束时的在线最后层统计为：

| 指标 | 从头训练 | 普通迁移 | 评价网络重置 |
| --- | ---: | ---: | ---: |
| 最后层特征秩 | 9.33 | 10.33 | 11.67 |
| 在线覆盖率 | 0.873 | 0.897 | 0.949 |
| 神经元休眠比例 | 0.596 | 0.587 | 0.578 |

普通迁移没有表现出更低的最后层秩或更高的休眠比例，在线覆盖率甚至高于从头训练。因此，行为失败不能由简单的“维度消失”或“神经元全部休眠”解释。

### 10.4 完整雅可比揭示谱集中与方向性减速

固定锚点上的完整雅可比分析得到：

| 指标 | 从头训练 | 普通迁移 | 评价网络重置 |
| --- | ---: | ---: | ---: |
| 最终谱熵有效秩 | 14.42 | 9.90 | 16.48 |
| 最终完整雅可比覆盖率 | 0.984 | 0.931 | 0.982 |
| 后半程归一化逆谱负担 | 16.52 | 70.48 | 20.62 |
| 最终归一化逆谱负担 | 15.51 | 69.20 | 18.35 |

普通迁移的最终谱熵有效秩最低，说明其学习谱更集中。更关键的是，它在任务 $B$ 后半程的逆谱负担分别是从头训练的 $4.27$ 倍、评价网络重置的 $3.42$ 倍，并且 3/3 个种子上都高于另外两组。

组平均逆谱负担随任务 $B$ 训练的演化为：

| 任务 B 步数 | 从头训练 | 普通迁移 | 评价网络重置 |
| ---: | ---: | ---: | ---: |
| 10 万 | 32.09 | 26.29 | 22.77 |
| 50 万 | 23.64 | 32.80 | 32.04 |
| 100 万 | 21.82 | 42.57 | 28.92 |
| 150 万 | 17.85 | 61.88 | 22.13 |
| 200 万 | 17.51 | 64.22 | 22.52 |
| 250 万 | 15.22 | 86.61 | 19.47 |
| 300 万 | 15.51 | 69.20 | 18.35 |

这个时间过程很重要：普通迁移并不是只在切换瞬间不对齐，而是在任务 $B$ 训练中逐步进入更高的谱负担状态；从头训练和评价网络重置则逐渐形成更适合任务 $B$ 的学习谱。

### 10.5 谱负担与实际拟合速度、控制性能一致

统一局部更新试验的最终残余误差为：

| 拟合对象 | 从头训练 | 普通迁移 | 评价网络重置 |
| --- | ---: | ---: | ---: |
| 固定贝尔曼相关方向 | 0.00865 | 0.04877 | 0.01035 |
| 一般平滑方向 | 0.00773 | 0.08620 | 0.01354 |

普通迁移拟合固定贝尔曼相关方向的残余误差分别是从头训练的 $5.64$ 倍、评价网络重置的 $4.71$ 倍。一般平滑方向的拟合也明显变慢，说明旧任务导致的谱集中会损害一般可塑性；当这种谱退化作用到任务 $B$ 的控制相关方向时，它就转化为实际的负向迁移。

在全部 9 个“训练设置 $\times$ 种子”点上，成功率曲线面积与累计逆谱负担的斯皮尔曼秩相关系数为：

$$
\rho_{\mathrm{Spearman}}
\left(
\operatorname{AUC}_{\mathrm{success}},
\operatorname{SAB}
\right)
=
-0.90.
$$

即谱负担越高，任务 $B$ 学得越差。

### 10.6 第一阶段验证了什么

第一阶段已经形成以下三点一致证据：

1. **现象证据：**普通迁移在 3/3 个种子上产生稳定负向迁移，评价网络重置显著恢复学习；
2. **结构证据：**负向迁移没有伴随简单的最后层秩坍塌或休眠率升高，却伴随完整评价网络学习谱集中和逆谱负担升高；
3. **动力学证据：**高逆谱负担对应更慢的实际函数拟合，并与控制成功率呈强负相关。

因此，第一阶段支持的核心结论是：

> **普通顺序微调会使继承评价网络的学习谱集中；当新任务的贝尔曼相关函数方向落入弱学习子空间时，有限预算内的评价学习显著减速并形成负向迁移。评价网络重置通过解除这种谱路径依赖恢复学习。**

这里不需要把机制限定成“只有贝尔曼方向受损”。更完整的解释是：旧任务造成一般学习谱退化，而新任务的贝尔曼结构决定这种退化是否、以及多大程度上转化为控制负向迁移。

---

## 11. 第二阶段：跨任务对的预测性验证

第一阶段证明了该机制可以解释 `sweep-into → push-wall`。第二阶段检验它是否能够解释**同一任务历史之后，不同新任务为什么出现不同程度的迁移**。

### 11.1 受控分支设计

共同任务 $A$ 为 `sweep-into`。每个种子先训练任务 $A$ 300 万步并保存完整检查点，包括策略网络、在线评价网络、目标评价网络、优化器状态和温度参数。随后从同一个任务 $A$ 检查点分支到不同任务 $B$，各分支从空经验回放开始收集任务 $B$ 数据。

候选任务为：

| 任务编号 | 任务 B |
| ---: | --- |
| 39 | `push-wall` |
| 47 | `sweep` |
| 48 | `window-close` |
| 49 | `window-open` |
| 38 | `push` |
| 41 | `reach-wall` |
| 7 | `button-press-wall` |
| 13 | `door-close` |

每个任务 $B$ 都比较：

$$
\text{从头训练 }B
\qquad\text{与}\qquad
A\rightarrow B\text{ 普通迁移}.
$$

所有任务均训练 300 万步，以获得可以直接用于论文的完整学习曲线。

### 11.2 主检验

对每个任务对计算迁移差距：

$$
\Delta_{A\rightarrow B}
=
\operatorname{AUC}_{A\rightarrow B}
-
\operatorname{AUC}_{\mathrm{scratch},B}.
$$

同时在固定的任务 $B$ 锚点上计算早期和全程累计逆谱负担。第二阶段的主预测是：

$$
\operatorname{Corr}
\left(
\operatorname{SAB}_{A\rightarrow B},
\Delta_{A\rightarrow B}
\right)
<0.
$$

这意味着：同一个任务 $A$ 检查点可以对某个任务 $B$ 产生严重负向迁移，对另一个任务 $B'$ 接近中性甚至产生正向迁移；决定差异的不是任务 $A$ 使网络“全局坏掉”，而是继承学习谱与不同新任务贝尔曼相关结构的匹配程度。

### 11.3 筛选与正式确认

先使用种子 0 对 8 个候选任务完成全量筛选，根据 $\Delta_{A\rightarrow B}$ 选出：

- 2 个强负向迁移任务；
- 2 个近中性任务；
- 2 个高迁移或正向迁移任务。

随后对选出的 6 个任务补齐 3 个种子。正式分析同时报告：

- 成功率曲线与迁移差距；
- 早期、后期和累计逆谱负担；
- 谱熵有效秩与完整雅可比覆盖率；
- 一般方向和贝尔曼相关方向的短程拟合速度；
- 跨任务、跨种子的谱负担—迁移差距关系。

第二阶段完成后，论文的主张将从“解释一个经典失败任务对”推进到“用任务条件谱负担预测跨任务负向迁移”。

---

## 12. 第三阶段：函数保持的谱因果干预

评价网络重置能够恢复学习，但它同时改变了当前价值函数、已有知识和学习谱，因此更适合作为正对照，而不是最干净的谱因果证明。第三阶段需要在**保持当前价值函数输出不变**的前提下，只改变评价网络的切线学习谱。

### 12.1 利用 ReLU 正齐次性的函数保持重参数化

对一个 ReLU 隐藏单元：

$$
h_j(x)=\operatorname{ReLU}(w_j^{\top}x+b_j),
$$

其输出层系数为 $a_j$。对任意 $c_j>0$，令：

$$
w_j'=c_jw_j,
\qquad
b_j'=c_jb_j,
\qquad
a_j'=\frac{a_j}{c_j}.
$$

由于 ReLU 的正齐次性：

$$
a_j'\operatorname{ReLU}
\left({w_j'}^{\top}x+b_j'\right)
=
a_j\operatorname{ReLU}
\left(w_j^{\top}x+b_j\right).
$$

因此重参数化前后的价值函数完全相同：

$$
Q_{\theta'}(s,a)=Q_{\theta}(s,a),
$$

但参数雅可比和切线核可以发生变化：

$$
J_{\theta'}\neq J_{\theta},
\qquad
K_{\theta'}\neq K_{\theta}.
$$

这提供了一个直接干预“学习谱”而不改变“当前函数”的工具。

### 12.2 定向谱提升目标

从共同任务 $B$ 锚点估计贝尔曼相关更新子空间，并选择缩放系数 $c=\{c_j\}$，最小化归一化逆谱负担：

$$
\min_{c_j>0}
\quad
\widetilde{\mathcal{P}}_B
\left(K(c),D_B\right)
+
\beta\sum_j\left(\log c_j\right)^2.
$$

第二项限制重参数化幅度；函数输出保持由网络结构和参数变换精确保证。

### 12.3 因果对照组

| 设置 | 目的 |
| --- | --- |
| 普通迁移 | 给出原始负向迁移基线 |
| 评价网络重置 | 给出能够恢复学习的正对照，但允许改变当前价值函数 |
| 随机函数保持重参数化 | 匹配参数变化幅度和核尺度，检验任意重参数化是否有效 |
| 面向任务 B 的定向谱提升 | 在不改变当前价值函数的条件下降低 $\widetilde{\mathcal{P}}_B$ |
| 反向谱抑制 | 将任务 B 的相关方向推向更弱谱区域，作为负对照 |

预期顺序为：

$$
\widetilde{\mathcal{P}}_B^{\mathrm{提升}}
<
\widetilde{\mathcal{P}}_B^{\mathrm{普通}}
<
\widetilde{\mathcal{P}}_B^{\mathrm{抑制}},
$$

并对应：

$$
\operatorname{AUC}^{\mathrm{提升}}
>
\operatorname{AUC}^{\mathrm{普通}}
>
\operatorname{AUC}^{\mathrm{抑制}}.
$$

### 12.4 所需时间顺序

最有说服力的因果证据应满足：

```text
干预时刻的价值函数输出保持不变
              ↓
逆谱负担立即按设计升高或降低
              ↓
前 1 万—10 万步的相关方向拟合速度改变
              ↓
随后控制成功率与学习曲线发生改变
```

如果定向谱提升优于核尺度匹配的随机重参数化，而反向抑制稳定损害学习，就能说明真正起作用的是**任务相关方向上的谱重分配**，而不是简单增大参数或梯度尺度。

---

## 13. 可证伪预测

### 预测一：谱集中不是充分条件

仅有较低的谱熵有效秩并不必然造成负向迁移。只有当任务 $B$ 的贝尔曼相关方向落入小特征值子空间时，学习才会显著变慢。

### 预测二：逆谱负担比无条件秩更能预测任务对差异

跨任务对分析中，$\operatorname{SAB}_{A\rightarrow B}$ 应比最后层特征秩、休眠比例和无条件谱秩更稳定地预测 $\Delta_{A\rightarrow B}$。

### 预测三：谱负担变化先于行为变化

普通迁移中的逆谱负担应在控制成功率分化之前升高；谱恢复干预应先改善局部拟合速度，再改善行为表现。

### 预测四：方向性比整体尺度更重要

在匹配切线核平均特征值、参数改变量和当前价值输出后，面向任务 $B$ 的定向谱提升仍应优于随机重参数化。

### 预测五：同一历史对不同新任务产生不同结果

固定任务 $A$ 和同一检查点后，不同任务 $B$ 的迁移差距应显著不同，并随任务条件谱负担系统变化。

---

## 14. 论文最终论证结构

整篇论文应形成以下证据链：

```text
普通顺序微调出现任务对相关的负向迁移
                    ↓
传统秩、休眠率和无权覆盖率不能稳定解释该现象
                    ↓
继承评价网络的完整切线学习谱发生集中
                    ↓
新任务贝尔曼相关方向落入弱谱区域，逆谱负担升高
                    ↓
逆谱负担预测实际拟合速度和跨任务迁移差距
                    ↓
函数保持的定向谱干预改变后续学习结果
                    ↓
形成对持续强化学习负向迁移的新解释与解决方法
```

---

## 15. 预期论文贡献

1. 提出**贝尔曼条件谱可塑性失配**，把持续强化学习中的一般可塑性退化与新任务的控制结构统一到同一任务条件谱框架中。
2. 提出**归一化逆谱负担**，区分“相关方向可以表示”与“相关方向能在有限预算内快速学会”。
3. 通过单任务对三种子实验和跨任务对验证，展示评价网络谱集中、相关方向拟合减速与行为负向迁移之间的系统关系。
4. 通过函数保持的定向谱干预，在不改变当前价值函数的前提下检验学习谱的因果作用，并据此形成定向谱恢复方法。

当前第一阶段已经在 `sweep-into → push-wall` 上验证了第 1、2 点，并为第 3 点提供了单任务对证据；第二阶段正在补充第 3 点的跨任务预测性；第三阶段对应第 4 点的因果验证与方法部分。

---

## 16. 当前最准确的研究主张

> **持续强化学习中的普通微调会使继承评价网络的切线学习谱发生路径依赖性的集中。谱集中造成一般可塑性下降，但它是否转化为控制负向迁移，取决于新任务的贝尔曼相关函数结构是否落入网络的弱学习方向。归一化逆谱负担刻画了这种任务结构与网络学习谱的失配，并在第一阶段三种子实验中同时对应评价拟合减速和行为失败。**

这个主张既不同于“所有负向迁移都是全局可塑性丧失”，也不同于“只有贝尔曼方向受损”。更准确的统一观点是：

$$
\boxed{
\text{一般谱可塑性决定网络还能如何学习，}
\qquad
\text{贝尔曼结构决定哪些学习方向会影响控制。}
}
$$
